apiVersion: apps/v1
kind: Deployment
metadata:
  name: pipe5
  namespace: stage
spec:
  replicas: 2
  selector:
    matchLabels:
      app: pipe5
  template:
    metadata:
      labels:
        app: pipe5
    spec:
      containers:
      - name: pipe5
        image: royjith/cube:version-3.19
        ports:
        - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  namespace: stage
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: pipe5
